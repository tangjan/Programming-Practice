// 常用的变量名称

#include <array>
#include <stack>
#include <string>
#include <vector>
using namespace std;

int main()
{
    bool flag;
    int ans, answer, col, count, empty, flag, in, input, k, len, length, m, n, number, nums, out, output, res, result, row, sum, test, times;

    // https://softwareengineering.stackexchange.com/questions/69788/what-is-the-history-of-the-use-of-foo-and-bar-in-source-code-examples
    // https://en.wikipedia.org/wiki/List_of_military_slang_terms#FUBAR
    int foo, bar;

    string str;
    array<int, 10> arr;
    stack<int> stk;
    vector<int> vec;
    
}